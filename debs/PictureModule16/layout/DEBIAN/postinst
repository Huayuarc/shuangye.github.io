#!/bin/sh
set -e
for d in /var/mobile/Library/PictureModule /var/jb/var/mobile/Library/PictureModule; do
    if [ -d "$(dirname "$d")" ]; then
        mkdir -p "$d"
        chown mobile:mobile "$d" 2>/dev/null || true
        chmod 775 "$d" 2>/dev/null || true
    fi
done
exit 0
