{
    Filter = {
        Mode = Any;
        Classes = (
            UIApplication
        );
        Bundles = (
            "com.apple.UIKit"
        );
        Executables = (
            SpringBoard,
            Preferences
        );
    };
}
