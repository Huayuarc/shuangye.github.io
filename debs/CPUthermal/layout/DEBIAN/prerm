#!/bin/sh
case "$1" in
remove|deconfigure)
/var/jb/usr/local/bin/CPUthermalChargeTool reset 2>/dev/null || true
launchctl bootout system/com.huayuarc.cputhermal.charge 2>/dev/null || true
# 兜底：工具缺失/旧包升级时清除当前方案固定目录（绝不删除 /System 原目录）。
;;
esac
exit 0
