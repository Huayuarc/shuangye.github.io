#!/bin/sh
case "$1" in
remove|deconfigure)
# 卸载清理：进程已退出时必然处于删除阶段无需重启；只做幂等收尾。
launchctl bootout system/com.huayuarc.cputhermal.mount 2>/dev/null || true
# 只清理本包独占资源目录；不操作共享父目录 /usr/local/share。
rm -rf @JBROOT@/usr/local/share/CPUthermal 2>/dev/null || true
status=$?
exit $status
;;
esac
exit 0
